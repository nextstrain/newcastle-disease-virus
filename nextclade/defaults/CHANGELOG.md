## Unreleased

 - initial release of draft NDV datasets
 - each dataset carries the reference of the other class as an outgroup, so that a sequence of the other class attaches to that one tip and is reported with the other class' `clade_membership` and no genotype
